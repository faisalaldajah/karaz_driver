import 'package:flutter/material.dart';

class UnRegistration extends StatelessWidget {
  static const String id = 'UnRegistration';

  const UnRegistration({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wait to be confirmed'),
      ),
    );
  }
}
